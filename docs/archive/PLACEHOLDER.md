# Resume PDF Placeholder

Place your actual resume PDF in this folder as `resume.pdf`.

The resume window will display a download button and can optionally show the PDF inline.

To enable inline PDF viewing, uncomment the iframe in `js/desktop.js` in the `openResume()` function.

**Note:** Some browsers may not support inline PDF viewing, so the download button is always available as a fallback.
